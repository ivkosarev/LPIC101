1) Просмотреть информацию о процессоре и модулях оперативной памяти.
cd /proc; cat cpuinfo; cat meminfo

2) Определить модель жесткого диска.
sudo lshw -short -C disk | grep sda

3) Вывести сведения обо всех платах расширения на шине PCIEx.
lspci | grep -i exp

4) Отключить звуковую карту.
-в моем случае-
cat /proc/modules | grep sound; 
sudo modprobe -r soundwire_intel soundwire_cadence soundwire_bus 

5) Выключить контроллер usb.
lsmod | grep usb
modinfo usbhid
sudo modprobe -r usbhid

-----------------------------
101_2_dz.txt

1) Выяснить какой загрузчик ОС использется в вашей системе.
cd /boot; ls

2) Просмотреть журнал событий ядра.
cat /var/log/dmesg

3) Изменить носитель по умолчанию, используемый для загрузки ОС.
#ставим граб на уже размеченную флешку
sudo grub-install --no-floppy --root-directory=/media/flash /dev/sdb1

4) Попробовать загрузить ОС в режиме, отличающемся от режима по умолчанию.
sudo grub-reboot 1

5) Узнать какой из стилей инициализации используется в вашей ОС.
ps -p 1 #где p это pid
----------
101_3_dz.txt
1) Изменить режим запуска по умолчанию ОС.
systemctl get-default
systemctl set-default multi-user.target

2) Переключиться между несколькими режимами работы.
runlevel #посмотреть
telinit 5 #сменить

3) Убрать из автозапуска по умолчанию службу сети.
systemctl disable systemd-networkd.service

4) Проверить статус службы аудио.
sudo systemctl status alsa-restore.service  alsa-state.service    alsa-utils.service

5) Перезагрузить ПК через работу с уровнями выполнения или таргетами.
systemctl isolate reboot.target
------
102_1_dz.txt
1) Подключить пустой диск и создать на нем два основных и два логических раздела.
sudo fdisk -l #list -смотрим все диски и разделы
sudo fdisk /dev/sdb
m-menu
n-new
p-primary partition
1-дефолтный номер раздела
Первый сектор по дефоту(пустое поле, enter)
+2GB
#Создали праймари, теперь екстендед
n
e
2
первый сектор по дефолту(пустое поле, enter), идет сразу за теми 2Гб что мы смотнировали
+2GB
#теперь создаем логический раздел
n
p
w-write, записали изменения
#и еще один логический
n
#все место заюзано, больше не предлагает тип раздела, сразу автоматом понимает что будет логический
1й сектор по дефолту
2й + 1GB
w
# повторяем, задание требует 2 логических,итого имеем
Device     Boot   Start      End Sectors  Size Id Type
/dev/sdb1          2048  3907583 3905536  1.9G 83 Linux
/dev/sdb2       3907584  7813119 3905536  1.9G  5 Extended
/dev/sdb3       7813120 10485759 2672640  1.3G 83 Linux
/dev/sdb5       3909632  5863423 1953792  954M 83 Linux
/dev/sdb6       5865472  7813119 1947648  951M 83 Linux

2) Смонтировать в них несколько пустых корневых каталогов.

3) Перенести каталог /home на отдельный раздел.
sudo -s # нужен рут
mkfs.ext4 /dev/sdb1 # форматнем раздел
mkdir /mnt/home 
mount /dev/sdb1 /mnt/home  # примонтировали каталог
cp -a /home/* /mnt/home # скопировали все из /home в /mnt
rm -rf /home/* # удалили старую папку /home
echo "/dev/sdb1       /home           ext4    defaults           0       1" >> /etc/fstab # допишем в fstab новое положение /home
mount -a # примонтировали все файловые системы что были указаны в fstab
df -h | grep /home # проверяем, на выходе имеем:
/dev/sdb1        1.9G          12M  1.7G            1% /home

4) Создать еще один раздел подкачки.
 fallocate -l 1G /swapfile1 # создали второй файл свопа с размером 1гиг
 chmod 600 /swapfile1 # ограничили права всем, кроме рута, руту дали только чтение
 mkswap /swapfile1 # отметили новый файл как пространство подкачки
 swapon /swapfile1 # автивировали своп
 swapon --show # проверили, итого имеем:
 NAME       TYPE   SIZE USED PRIO
/swapfile  file 947.2M   0B   -2
/swapfile1 file  1024M   0B   -3

5) Добавить еще два диска и создать на нем несколько логических томов (LVM).

6) Попробовать переносить тома с одного жесткого диска на другой без остановки ОС.

7) Определить точную версию ядра ОС.
uname -v
--------
102_2_dz.txt

1) Добавить таймаут на старт операционной системы в 10 секунд.
2) Изменить текст выбора операционной системы.
3) Попробовать подключить жесткий диск с Windows и указать вашему загрузчкику на возможность загружаться с добавленного диска.
4) Установить загрузку Windows опцией по умолчанию.
-------
102_3_dz.txt

1) Посмотреть текущие пути к библиотекам.
2) Добавить свою домашнюю папку к каталогам нахождения библиотек.
3) Рассмотреть библиотеки, используемые утилитой ifconfig.
-------
102_4_dz.txt

1) Добавить в репозитории информацию для работы с пакетом java.
2) Установить java со всеми зависимостями.
3) Удалить java.
4) Установить webmin.
5) Обновить все пакеты ПО. 
-------
102_5_dz.txt

1) Добавить в репозитории информацию для работы с пакетом java.
2) Установить java со всеми зависимостями.
3) Удалить java.
4) Установить webmin.
5) Обновить все пакеты ПО. 
